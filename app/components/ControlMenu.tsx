import React, { ReactElement } from "react";

interface Props {}

function ControlMenu({}: Props): ReactElement {
  return(
    <div>
      I am Control Menu
    </div>
  );
}

export default ControlMenu;
